# system_proxy

`system_proxy/` 负责在后端进程中打开和恢复系统 HTTP/HTTPS 代理。

## 子目录与文件

- `mod.rs`：系统代理开关入口、恢复快照保存、周期重应用与退出恢复；重复开启同一端口时保留原始快照，关闭时若没有快照则兜底关闭指向本机代理端口的系统代理。
- `network/`：按操作系统实现具体网络服务读写逻辑，目前 macOS 使用 `networksetup`。

## 维护要求

调整系统代理开关行为或新增平台实现时，必须同步更新本文件；对外接口变化需同步更新 `api.rs` 及对应测试。
