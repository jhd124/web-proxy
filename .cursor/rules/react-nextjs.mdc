---
globs: "*.ts,*.tsx"
alwaysApply: false
---

# React/Next.js 开发规范

## 基本原则
- 使用函数组件和 TypeScript 接口，`React.FC` 定义组件类型
- 声明式 JSX，函数式编程风格
- 用 `Suspense` 包裹客户端组件并提供 fallback
- 对非关键组件使用动态加载（`next/dynamic`）

## 组件设计模式：职责分离

页面和组件必须是 React 组件 + 自定义 Hook 的组合。**所有副作用、数据获取和业务逻辑放在 Hook 中；组件只负责渲染和组合。**

两种允许的模式：
1. **有状态组件 + 自定义 Hook**：组件仅保留最小 UI 状态
2. **无状态（展示型）组件**：从 Hook 或父组件接收数据/处理函数

**优先无状态组件**。允许例外：纯本地 UI 切换（如 `isOpen` 布尔值）可直接用 `useState`，需添加注释说明。

### ✅ 正确：业务逻辑在 Hook 中

```typescript
// useUser.ts
export function useUser(userId: string) {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    if (!userId) { setUser(null); setLoading(false); return; }
    let mounted = true;
    setLoading(true);
    fetchUser(userId)
      .then((data) => { if (mounted) { setUser(data); setError(null); } })
      .catch((err) => { if (mounted) setError(err); })
      .finally(() => { if (mounted) setLoading(false); });
    return () => { mounted = false; };
  }, [userId]);

  return { user, loading, error };
}

// UserCard.tsx - 只负责渲染
export const UserCard: React.FC<UserCardProps> = ({ userId }) => {
  const { user, loading, error } = useUser(userId);
  if (loading) return <Loading />;
  if (error) return <Error message={error.message} />;
  if (!user) return <Empty />;
  return <div className={styles.card}><h3>{user.name}</h3></div>;
};
```

### ✅ 允许例外：纯 UI 状态

```typescript
// 对于纯粹的 UI 切换，创建 Hook 会过度工程化，直接 useState 即可
export const ToggleButton: React.FC = () => {
  const [isOpen, setIsOpen] = useState(false);
  return <button onClick={() => setIsOpen((prev) => !prev)}>{isOpen ? 'Open' : 'Closed'}</button>;
};
```

## Next.js 特定规范

- **优先 Server Components**，最小化 `'use client'` 使用范围
- `'use client'` 只用于需要浏览器 API 或直接交互的小型组件，不用于数据获取
- Server Components 中用 `async/await` 直接获取数据；并行数据用 `Promise.all`
- 导航使用 `next/link` 的 `<Link>` 和 `useRouter`；**禁止** `window.location.href`（破坏 i18n）

## 性能优化

- 对重型或频繁重渲染的组件用 `React.memo`，设置 `displayName`
- 缓存昂贵计算用 `useMemo`，稳定回调用 `useCallback`
- 非关键组件用 `dynamic(() => import(...), { loading: () => <p>加载中...</p> })`
- 大列表用虚拟化（`react-virtuoso`）

## 自定义 Hook 规范

1. Hook 必须以 `use` 开头
2. 只在函数组件或自定义 Hook 顶层调用 Hook
3. 不在循环、条件或嵌套函数中调用 Hook
4. 依赖数组必须完整正确

---

## React Hooks 调用顺序规范（重要）

React 要求每次渲染 Hook 调用次数和顺序完全一致。违反此规则导致运行时状态错乱，Biome 报 `lint/correctness/useHookAtTopLevel`。

### ❌ 违规 1：Early return 在 Hook 之前

```typescript
// ❌ 错误
const MyComponent = ({ data }: Props) => {
  if (!data) return null;               // 提前 return
  const [state, setState] = useState(false); // Hook 在条件之后 ❌
  return <div>{state}</div>;
};

// ✅ 正确：Hook 前置，条件 return 后移
const MyComponent = ({ data }: Props) => {
  const [state, setState] = useState(false);
  if (!data) return null;
  return <div>{state}</div>;
};
```

### ❌ 违规 2：冗余的 SSR 守卫

在 `'use client'` 组件中，`typeof window === 'undefined'` 通常是多余的，且若放在 Hook 之前会违规。

```typescript
// ❌ 错误：守卫在 Hook 之前
'use client';
const MyComponent = () => {
  if (typeof window === 'undefined') return null;
  const [open, setOpen] = useState(false); // Hook 在守卫之后 ❌
  return <div>{open && <Modal />}</div>;
};

// ✅ 正确：移除冗余守卫
'use client';
const MyComponent = () => {
  const [open, setOpen] = useState(false);
  return <div>{open && <Modal />}</div>;
};
```

判断守卫是否多余：`useState/useReducer/useMemo/useCallback/useEffect` 均 SSR 安全；DOM 相关 Hook 若内部有 `typeof document !== 'undefined'` 守卫则安全。

### ❌ 违规 3：条件调用 Hook

```typescript
// ❌ 错误
const data = enabled ? useMyHook() : undefined;

// ✅ 正确：无条件调用，条件控制使用
const hookData = useMyHook();
const data = enabled ? hookData : undefined;
```

### ❌ 违规 4：Hook 依赖 early return 的数据

```typescript
// ❌ 错误：useCallback 放在守卫之后
const MyComponent = ({ dataId }: Props) => {
  const { data, handler } = useMemo(() => resolve(dataId), [dataId]);
  if (!data || !handler) return <Fallback />;
  const handleClick = useCallback(() => { handler.onClick(data); }, [data, handler]); // ❌
  return <div onClick={handleClick} />;
};

// ✅ 正确：Hook 前置，守卫逻辑移入 Hook 内部
const MyComponent = ({ dataId }: Props) => {
  const { data, handler } = useMemo(() => resolve(dataId), [dataId]);
  const handleClick = useCallback(() => {
    if (!data || !handler) return; // 守卫移入内部
    handler.onClick(data);
  }, [data, handler]);
  if (!data || !handler) return <Fallback />; // 必须在所有 Hook 之后
  return <div onClick={handleClick} />;
};
```

同理适用于 `useEffect`：把条件判断移入 effect 内部，不要把 `useEffect` 放到条件 return 之后。

### ❌ 违规 5：useState setter 命名误导 Biome

```typescript
// ❌ 以 use 开头的 setter 会被误识别为 Hook
const [ref, useRef] = useState<HTMLElement | null>(null);

// ✅ setter 使用 set 前缀
const [ref, setRef] = useState<HTMLElement | null>(null);
```

---

## 强制行为 ✅
1. 业务逻辑必须提取到自定义 Hook
2. 所有 Props 和 State 必须有 TypeScript 类型
3. 必须处理 loading、error、空状态
4. 组件文件 < 300 行
5. 优先 Server Components
6. `useEffect` 依赖数组必须正确
7. 所有 Hook 调用置于组件顶层，任何 `return` / 条件判断之前

## 禁止行为 ❌
1. 禁止在组件中直接写复杂业务逻辑
2. 禁止在组件中直接调用 API（用 Hook 或 Server Component）
3. 禁止在 JSX 中写复杂逻辑
4. 禁止忽略 useEffect 依赖警告
5. 禁止滥用 `'use client'`
6. 禁止在 Server Component 中使用浏览器 API
7. 禁止在任何 Hook 调用之前放置 `return` 或条件判断
8. 禁止在 `'use client'` 顶部用 `typeof window === 'undefined'` 做提前返回守卫
9. 禁止 `useState` 的 setter 命名为 `useXxx`
