---
alwaysApply: true
---
# project

* The backend should take more responsiblities, and the fontend should be as light-wight as posible

# backend
* Rust crate: `backend/`；仓库根 workspace 含 `backend` 与 `desktop/src`；在根目录执行 `cargo build -p proxy-app` 等即可。

# 桌面 (Tauri 2)
* 目录为 `desktop/`：其中 `package.json` + `src/`。在 `desktop/` 执行 `npm install`，再 `npm run tauri:dev` 或 `npm run tauri:build`；或根目录 `make tauri-dev` / `make tauri-build`。sidecar 跑 `proxy-app`，WebView `http://127.0.0.1:9091`；`PROXY_DATA_DIR` 为应用 support 数据目录。

# frontend

## components
 * basic component library is shadcn
 * custom components is in src/components 

* 持久操作使用toast给出反馈

## 技术栈
* 包管理器: bun
* UI: react
* 打包: vite