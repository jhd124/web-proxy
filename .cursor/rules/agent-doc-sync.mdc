# Agent 文档同步规则（模块介绍文档）

当 Agent 在本仓库实现、修改或删除功能时，必须同步维护模块介绍文档，保证文档与实现一致。

## 适用范围

以下目录发生功能性改动时触发：

- `backend/`
- `frontend/`
- `desktop/`
- `mcp/`

## 强制行为 ✅

1. 改动 `backend/` 后，必须检查并按需更新 `docs/agent-reference/backend.md`
2. 改动 `frontend/` 后，必须检查并按需更新 `docs/agent-reference/frontend.md`
3. 改动 `desktop/` 后，必须检查并按需更新 `docs/agent-reference/desktop.md`
4. 改动 `mcp/` 后，必须检查并按需更新 `docs/agent-reference/mcp.md`
5. 若新增跨模块流程（例如 API + UI + Electron IPC 联动），必须同时更新受影响的多份文档
6. 若新增目录级能力但尚无对应介绍文档，必须先补文档再结束任务

## 更新标准

文档更新至少覆盖以下变更中的相关项：

- 技术架构或依赖变化
- 核心功能模块变化
- 对外接口（API / 命令 / tool）变化
- 关键数据结构或状态流变化
- 运行方式、构建方式、环境变量变化

## 禁止行为 ❌

1. 只改代码不改介绍文档
2. 在文档中保留已失效的接口、字段或流程描述
3. 用笼统表述替代具体实现变化（如“优化了一些逻辑”）
