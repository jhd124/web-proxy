# headers-table

通用 headers 表格展示组件。

## 组件

- `HeadersTable`：以双列 grid 渲染 `[key, value][]` 格式的 HTTP headers，key 用 accent 色高亮。

## 使用方

- `features/saved-requests/ui/SavedRequestsPanelUI.tsx`
- `features/traffic/ui/TrafficPanelUI.tsx`
