# page-search/ui

`page-search/ui/` 存放页面检索模块的纯展示组件与样式。

## 当前 UI

- `PageSearchUI.tsx`：右下角浮动搜索框，包含右侧前后结果跳转按钮与关闭按钮，并展示当前关键词的匹配数量、当前跳转位置或无匹配状态。
- `PageSearchUI.module.css`：搜索框浮层、输入框、图标按钮和匹配状态样式。

## 维护要求

新增、删除或重构 UI 组件时，必须同步更新本文件。
