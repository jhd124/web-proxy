# hosts/ui

`ui/` 提供 hosts 管理页的纯展示组件。

## 文件说明

- `HostsPanelUI.tsx`：展示系统 hosts 状态、托管条目表单、保存、应用到系统和移除托管区块操作。
- `HostsPanelUI.module.css`：页面布局、表格编辑器和状态标签样式。

## 维护要求

调整 Hosts 页面交互、表单字段或样式结构时，必须同步更新本文件。
