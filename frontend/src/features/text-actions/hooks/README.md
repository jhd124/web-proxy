# text-actions/hooks

`text-actions/hooks/` 存放文本动作相关 Hook。

## 当前 Hook

- `useTextContextActions.ts`：读取 `TextActionsProvider` 提供的页面搜索、全局搜索、Decode/Format 与浏览器搜索动作。

## 维护要求

新增或调整文本动作 Hook 时，必须同步更新本文件。
