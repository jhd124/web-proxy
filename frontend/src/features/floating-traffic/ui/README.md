# floating-traffic/ui

`floating-traffic/ui/` 存放悬浮流量详情展示组件与样式。

## 当前 UI

- `FloatingTrafficUI*`：悬浮容器。
- `FloatingTrafficDetailPanelUI*`：详情侧板。
- `FloatingTrafficDetailDrawerUI.tsx`：详情抽屉。

## 维护要求

新增或调整悬浮详情 UI 组件时，必须同步更新本文件。
