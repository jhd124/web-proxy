# breakpoints/ui

`breakpoints/ui/` 存放断点面板展示组件与样式。

## 当前 UI

- `BreakpointsPanelUI.tsx`：断点面板主体。
- `BreakpointsPanelUI.module.css`：基础样式。
- `BreakpointsPanelUI.overlay.module.css`：覆盖层样式。

## 维护要求

新增或调整断点 UI 组件与样式时，必须同步更新本文件。
